name: Publish portable app

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  windows-portable:
    name: Windows portable app
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Test
        shell: pwsh
        run: |
          dotnet test RouteAnalyzer.sln

      - name: Publish app
        shell: pwsh
        run: |
          ./scripts/publish-app.ps1 -Runtime win-x64 -Configuration Release

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: RouteAnalyzer-win-x64
          path: artifacts/app/win-x64/RouteAnalyzer.exe

      - name: Upload release asset
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "${{ github.ref_name }}"
          $asset = "artifacts/app/win-x64/RouteAnalyzer.exe"
          gh release view $tag *> $null
          if ($LASTEXITCODE -ne 0) {
            gh release create $tag $asset --title $tag --generate-notes
          }
          else {
            gh release upload $tag $asset --clobber
          }
